-- This is an undocumented module. I deiceded to scrap this module, and a beter module will come in the future.
--[[
	You'll need:
		- DateTime and TimeSpan module

	and please insert path to DateTime module in "INSERT DATETIME MODULE PATH HERE"
]]
return
{
	DateTime = require(script.DateTime);
	
	Copy = require(script.Copy);
	
	BigInteger = require(script.BigInteger);
	Decimal = require(script.Decimal);
};